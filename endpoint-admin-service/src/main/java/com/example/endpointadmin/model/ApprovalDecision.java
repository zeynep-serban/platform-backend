package com.example.endpointadmin.model;

/**
 * BE-017 — the decision a second admin records on a destructive command
 * pending dual-control approval.
 */
public enum ApprovalDecision {
    APPROVE,
    REJECT
}
